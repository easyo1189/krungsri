# สำหรับ deploy บน Render
# Database connection variables (provided by Neon PostgreSQL)
DATABASE_URL=${DATABASE_URL}

# Session secret (for securing cookies and session data)
SESSION_SECRET=cashluxe-session-secret-1234

# Environment mode ("development" or "production")
NODE_ENV=production

# System backup key (for emergency data restore)
SYSTEM_RESTORE_KEY=cashluxe-emergency-restore-key
